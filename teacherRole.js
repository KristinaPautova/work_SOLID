class TeacherRole {
    constructor(subject,teacher) {
        this.subject = subject;
        this.teacher = teacher;
    }

    setSubject(subject) {
        this.subject = subject;
    }

    setTeacher(teacher) {
        this.teacher = teacher;
    }
}